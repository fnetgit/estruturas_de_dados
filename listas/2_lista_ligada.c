// A lista simplesmente encadeada é composta por nós onde cada nó contém:
// 1. Um valor (dado que queremos armazenar).
// 2. Um ponteiro que aponta para o próximo nó da lista (ou NULL se for o último).
// Isso permite inserir e remover elementos de forma dinâmica, mas só pode ser percorrida de forma unidirecional (do início para o fim).

#include <stdio.h>
#include <stdlib.h>
